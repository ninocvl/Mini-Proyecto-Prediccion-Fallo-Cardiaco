apiVersion: apps/v1
kind: Deployment
metadata:
  name: heart-model
spec:
  replicas: 1
  selector:
    matchLabels:
      app: heart-model
  template:
    metadata:
      labels:
        app: heart-model
    spec:
      containers:
      - name: model
        image: heart-api:latest           # ← Imagen CORRECTA
        imagePullPolicy: IfNotPresent     # ← Política CRÍTICA para usar la imagen local
        ports:
        - containerPort: 8000